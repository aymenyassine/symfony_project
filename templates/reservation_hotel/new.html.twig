{% extends 'base.html.twig' %}

{% block title %}Nouvelle Réservation{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Nouvelle Réservation</h1>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-body">
                        {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

                        <div class="mb-3">
                            {{ form_label(form.date, 'Date de réservation', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.date, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.date) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.guest_name, 'Nom du client', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.guest_name, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.guest_name) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.numberOfGuests, 'Nombre de clients', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.numberOfGuests, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.numberOfGuests) }}
                        </div>

                        <!-- Ajout du champ pour la sélection de l'hôtel -->
                        <div class="mb-3">
                            {{ form_label(form.Hotel, 'Sélectionner un hôtel', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.Hotel, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.Hotel) }}
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">Réserver</button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
