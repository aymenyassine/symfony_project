{% extends 'base.html.twig' %}

{% block title %}ReservationHotel{% endblock %}

{% block body %}
    <h1>ReservationHotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_hotel.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation_hotel.date ? reservation_hotel.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Guest_name</th>
                <td>{{ reservation_hotel.guestName }}</td>
            </tr>
            <tr>
                <th>NumberOfGuests</th>
                <td>{{ reservation_hotel.numberOfGuests }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_reservation_hotel_index') }}">back to list</a>

    <a href="{{ path('app_crud_reservation_hotel_edit', {'id': reservation_hotel.id}) }}">edit</a>

    {{ include('crud_reservation_hotel/_delete_form.html.twig') }}
{% endblock %}
