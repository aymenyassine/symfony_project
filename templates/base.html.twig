<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {# Bootstrap link css #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <title>{% block title %}Project symfony{% endblock %}</title>
        <style>
            .logo {
                width: 150px;
            }
            .collapse{
                justify-content: space-between;
            }
            .navbar {
                padding: 0.5rem 1rem;
            }
            .container-fluid {
                display: flex;
                align-items: center; /* Aligne tout verticalement */
                justify-content: space-between; /* Logo à gauche, formulaire à droite */
            }
            .navbar-nav {
                flex: 1; /* Permet au menu de s'étendre au centre */
                display: flex;
                justify-content: center; /* Centre les liens du menu */
            }
            body {
                display: flex;
                flex-direction: column;
                min-height: 100vh;
                margin: 0;
            }
            main {
                flex: 1;
            }
            footer {
                background-color: #343a40;
                color: white;
                text-align: center;
                
                
            }
            .hotel-image{
                width: 300px;
                height: 300px;
                border-bottom: 1px solid #ddd;
                margin-bottom: 1.5rem;
            }
            footer p{
                padding-top: 10px;
            }
        </style>
        {% block stylesheets %} {% endblock %}
    </head>
    <body>
        {# Navbar #}
        <nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
            <div class="container-fluid">
                <a href="{{ path('app_home')}}"><img src="{{asset('img/logo.svg')}}" alt="logo" class="logo"></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('app_home')}}">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_hotel')}}">Hôtel</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Activite
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="{{ path('app_restaurent_index')}}">Restaurents</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_piscine')}}">Piscine</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_excursions')}}">Excursions</a></li>
                            </ul>
                        </li>
                            {% if not app.user %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_register')}}">Register</a>
                                    <li class="nav-item">
                                        <a class="nav-link" href="{{ path('app_login')}}">Login</a>
                                    </li>
                                </li>
                            {% else %}
                                <li class="nav-item">
                                    {# <a class="nav-link" href="#">Edit</a> #}
                                    {# role Admin #}
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_logout')}}">Logout</a>
                                </li>
                                
                            {% endif %}
                    </ul>
                    <form class="d-flex" role="search">
                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-warning" type="submit">Search</button>
                    </form>
                </div>
            </div>
        </nav>

        <main>
            {% block body %}{% endblock %}
                
            
        </main>
        <footer class="bg-dark text-white text-center py-3">
            <p>&copy; 2025 hotels. All Rights Reserved by ARA.</p>
        </footer>
        {# Bootstrap link js #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
        
        {% block javascripte %}{% endblock %}
    </body>
</html>